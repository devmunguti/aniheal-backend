# Aniheal Backend API

Express & MongoDB REST API service.

## Setup & Running

1. Install dependencies:
```bash
npm install
```

2. Configure environment:
Copy `.env.example` to `.env` and adjust database/port settings if needed.

3. Run in development mode:
```bash
npm run dev
```

4. Run in production:
```bash
npm start
```
